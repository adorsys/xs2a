{{ if and .Values.cms.enabled (not .Values.cms.existingSecret ) }}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ template "psd2.fullname" . }}-cms
  labels:
{{ include "psd2.labels" . | indent 4 }}
type: Opaque
data:
{{- range $name, $value := .Values.cms.existingSecret }}
{{- if not (empty $value) }}
  {{ $name }}: {{ $value | toString | b64enc | quote }}
{{- end }}
{{- end }}
{{- end }}
